/**
 * Created by comdiv on 12.11.2014.
 */
define([],function() {
    require.config({
        paths: {
            // setup paths to modules to test here don't forget fully qualify paths from /tests folder
        }
    });
    return [
        //provide test module names here can be locally to /tests folder
    ]
});